Program to find the largest number using ternary operator and check whether the number is even or odd

public class JavaExercises{

public static void main(String[] args) {
		
		int num1, num2, num3, temp, result;
		
		Scanner input = new Scanner(System.in);
		System.out.println("Enter a first number");
		num1 = input.nextInt();
		
		System.out.println("Enter the second number");
		num2 = input.nextInt();
		
		System.out.println("Enter the three number");
		num3 = input.nextInt();
		
		input.close();
		
		temp = num1 > num2? num1 : num2;
		result = temp > num3? temp : num3;
		
		if(result%2 == 0) {
		System.out.println(result + " is even and the largest number!");
		}else {
			System.out.println(result + " is odd and the largest number!");
		}
	}
  }
